


/*
window.ondrop = function (e) {
    // don't replace document
    e.preventDefault();
    e.stopPropagation();

    // capture file
    var dt = e.dataTransfer;
    var files = dt.files;

    var file = files[0];
console.log(e.target);
console.log(dt);
console.log(files);

}
*/